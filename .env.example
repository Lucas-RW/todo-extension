# Supabase — same project used by both apps
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Copy the values above into:
#   apps/web/.env.local          (prefix with NEXT_PUBLIC_)
#   apps/extension/.env.development  (prefix with PLASMO_PUBLIC_)
